import 'package:flutter/cupertino.dart';

class CustomReservationCard extends StatelessWidget {
  const CustomReservationCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(

    );
  }
}
